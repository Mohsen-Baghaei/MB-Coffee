import { ReactElement } from "react";

const Address = (): ReactElement => {
  return (
    <div className="flex flex-col items-center justify-center w-full gap-7"></div>
  );
};

export default Address;
